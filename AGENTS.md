The project prohibits cheating. No false tests, no python launches in an isolated directory. The project should not turn into a JIT emulator. You don't have to do it under only 1 binary, so that it doesn't fit under the others. Be absolutely strict about development and do not try to do so as not to upset the user. He will be more upset by your deception. Never try to do the whole project in one step, but don't take a step on microtasks if it's not worth it. Comments, README, Documentation in English only. Always run through the Go analysis tools the purity of the code and the minimum number of allocations. If you need to download something, ask the user. In most cases, I won't mind. It is allowed to use the Internet
